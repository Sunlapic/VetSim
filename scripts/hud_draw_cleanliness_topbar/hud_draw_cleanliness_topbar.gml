/// hud_draw_cleanliness_topbar(_hud)
/// @description Нативное значение чистоты внутри верхнего HUD.
/// Пакет №148: отключено — чистота теперь рисуется в общей карточке
/// hud_draw_main_bars (пакет №147), чтобы не было двойного значения.

function hud_draw_cleanliness_topbar(_hud) {
    // Ничего не рисуем. Чистота показывается в hud_draw_main_bars.
}
